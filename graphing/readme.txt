i am not able to put json data(actual_JSON.intensity ) in data:[] of chart dataset function as it shows 
graph length and actual_JSON.country into labels:[] of chart dataset  as it shows the name of each element of graph
